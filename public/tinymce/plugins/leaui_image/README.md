`leaui_image` is Leanote's self-hosted TinyMCE 8 image-manager adapter.

The iframe uses the application album endpoints and communicates with the
editor through the TinyMCE 8 URL-dialog `postMessage` contract. No remote
editor or CDN runtime is required.
