# Leanote 领域上下文

本文档定义技术规划、任务、测试和评审中使用的稳定术语。实现细节与阶段计划保存在 Trellis 任务中，架构取舍保存在 `docs/adr/`。

## 核心领域术语

### 外部 API 契约

已发布桌面、iOS 和 Android 客户端依赖的 `/api/*` HTTP 行为，包括状态码、JSON 信封、字段名、字段类型、ObjectId 与时间格式，以及兼容客户端可观察到的字段顺序。技术栈迁移不得把这些变化当作内部重构细节。

### USN 同步契约

每个用户的单调递增更新序号及其增量同步语义。笔记、笔记本和标签的 mutation 必须递增 USN；`GetSync*` 必须能在对应增量中返回该变化。只更新数据而不递增 USN，等价于客户端不可见的写入。

### 用户所有权查询

把 `UserId` 与资源条件放在同一数据库查询中的授权边界，例如 `...ByIdAndUserId`。仅在 controller 或中间件里检查登录不等价于所有权校验；迁移查询时不得丢失该条件。

### Golden 基线

在真实 HTTP 边界录制的现有行为快照。它钉住当前行为而非宣称当前行为正确；易变字段只替换值并验证格式，不重排 JSON 键。发现既有缺陷时单独记录，不在基线任务中顺手修复。

### 源码资源

开发者直接维护、作为前端行为唯一事实来源的 JavaScript、CSS、模板、语言和第一方插件源码。

### 生成资源

由现代构建链从源码资源确定性生成并继续提交到 Git 的生产 bundle、压缩文件、语言文件和 `note.html`。生成资源不得手工修改；CI 重建后必须零 diff。

### 现代化门禁

一个迁移阶段合并前必须通过的阶段性验证，包括针对性测试、Golden 回放、构建、静态检查和必要的真实页面冒烟。父子关系不代表依赖；依赖由任务文档与 `task.json.meta.depends_on` 明确表达。

### 协调父任务

只维护共享约束、子任务地图和跨子任务验收，不直接修改生产代码的 Trellis 任务。根任务 `tech-stack-modernization` 与 `frontend-libs` 均采用这一角色。

## 不变量

- `/api/*` 外部契约与 USN 同步契约在现代化期间保持兼容。
- MongoDB collection 名、字段名和 BSON 标签保持不变，升级不要求数据迁移。
- 用户所有权查询始终在数据库查询条件中强制执行。
- 服务端渲染、现有 URL 结构、TinyMCE 编辑器内核和用户上传博客主题继续保留。
- 未编辑的存量笔记不得触发保存；编辑后允许列明的 HTML 规范化，但内容与 DOM 语义必须等价。
- 生成资源由脚本生成并接受漂移检查，不再维护并行的手工事实来源。

