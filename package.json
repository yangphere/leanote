{
  "name": "leanote-front-end-builder",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=24 <25"
  },
  "scripts": {
    "test": "node --test tests/js/*.test.js",
    "build": "node scripts/build/index.mjs",
    "test:e2e:build": "playwright test --config=playwright.config.mjs --project=build-smoke",
    "test:e2e": "playwright test --config=playwright.config.mjs --project=business"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1",
    "esbuild": "0.28.2",
    "jquery": "3.7.1",
    "jquery-migrate": "3.6.0"
  },
  "author": "life(http://life.leanote.com)",
  "license": "GPL v2",
  "bugs": {
    "url": "https://github.com/yangphere/leanote/issues"
  }
}
